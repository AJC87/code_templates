#!/bin/bash
# Install Kotlin compiler - kotlinc

# TODO: check JDK is installed, if not install

# Does this include kotlin command too
if ! command -v kotlinc &> /dev/null; then
# TODO: check if on macOS
# brew update
# brew install kotlin

# TOOD: add Linux installation
fi
